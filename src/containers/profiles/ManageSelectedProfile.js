import { connect } from 'react-redux';
import SingleProfileManagementPanel from '../../components/profiles/SingleProfileManagementPanel';

const mapStateToProps = (state) => ({});

const mapDispatchToProps = (dispatch) => ({});

export default connect(
    mapStateToProps,
    mapDispatchToProps
)(SingleProfileManagementPanel);